---
title: "DGX Spark 静默死机：长 Prefill 与 nvidia-580-open"
date: 2026-09-24 04:43:54
tags: ["gist", "summary"]
header: true
footer: true
author: "Ivar.Chen"
source: "https://iintothewind.github.io/"
gist_id: d1b592067fd3a51b9df485f1165b3085
---

# DGX Spark 静默死机：长 Prefill 与 nvidia-580-open

DGX Spark（GB10，128GB 统一内存）跑大模型**长上下文 prefill**（社区复现约 69K+ token）时，可能整机瞬间卡死：无 panic、无清晰 OOM、看门狗不触发，往往只能拔电。这和「推理进程挂了但还能 SSH」不是一类问题。

## 原因

部分出厂镜像默认是开源 GPU 内核模块：

```bash
modinfo nvidia | grep filename
cat /proc/driver/nvidia/version
```

若路径含 `nvidia-580-open`，或 NVRM 写着 `Open Kernel Module`，即属易感配置。社区多台对比结论：该模块在统一内存高压下的分配失败路径可能触发**内核活锁**；换成同版本**专有模块**后现象明显缓解，性能通常无明显回退。

短请求不易触发；偶发超长 prefill 更容易踩中。拉长框架超时、降并发只能减少频率，**不能代替换驱动**。

## 补丁

| 从 | 到 |
|---|---|
| `nvidia-driver-580-open` | `nvidia-driver-580` |
| `linux-modules-nvidia-580-open-nvidia-hwe-24.04` | `linux-modules-nvidia-580-nvidia-hwe-24.04` |

保持同一 580 系列，勿顺便跨大版本升级（驱动需与 OTA 固件配对）。先确认源里有包：

```bash
apt-cache policy nvidia-driver-580 nvidia-driver-580-open
apt-cache policy linux-modules-nvidia-580-nvidia-hwe-24.04
```

## 操作

```bash
# 1. 留档并停掉所有大模型负载
modinfo nvidia | grep -E '^(filename|version|license):'
cat /proc/driver/nvidia/version
docker stop <your-llm-containers...>

# 2. 换同版本专有模块
sudo apt update
sudo apt install -y nvidia-driver-580 linux-modules-nvidia-580-nvidia-hwe-24.04
# 如有残留：sudo apt purge -y nvidia-driver-580-open linux-modules-nvidia-580-open-nvidia-hwe-24.04

# 3. 重启
sudo reboot
```

## 确认

```bash
modinfo nvidia | grep filename
cat /proc/driver/nvidia/version
nvidia-smi
```

通过：`filename` 为 `.../nvidia-580/nvidia.ko`（无 `open`）；NVRM 为 `NVIDIA UNIX Kernel Module`（无 `Open`）；`nvidia-smi` 正常。

可选：专有模块下再跑一次长 prefill。若仍整机静默死机，需另查电源/固件，不能再单怪 open 模块。

回滚：

```bash
sudo apt install -y nvidia-driver-580-open linux-modules-nvidia-580-open-nvidia-hwe-24.04
sudo reboot
```

---

## 参考

1. 社区实操：长 prefill（约 69K+）静默死机，对比指向 `nvidia-580-open` 活锁；缓解为同版本专有模块并重启。  
2. [Silent hard-freeze under sustained inference](https://forums.developer.nvidia.com/t/dgx-spark-silent-hard-freeze-under-sustained-llama-cpp-inference-no-xid-oom-panic-10-second-telemetry-captured-warranty-ends-oct-14-requesting-cas/383624)  
3. [Spark hangs — hard-reset required](https://forums.developer.nvidia.com/t/spark-hangs-requires-a-hard-reset-physically-unplugging/358951)  
4. [Default / open driver notes](https://forums.developer.nvidia.com/t/dgx-spark-nvidia-driver-issue/351828) · [Apt upgrade broke driver](https://forums.developer.nvidia.com/t/apt-upgrade-has-broken-nvidia-driver/371799)  
5. [Driver vs OTA firmware pairing](https://forums.developer.nvidia.com/t/dgx-spark-apt-upgrade-to-driver-580-173-02-breaks-gpu-on-ota2607-nvidia-smi-no-devices-found/378200)  
6. [NVIDIA/open-gpu-kernel-modules](https://github.com/NVIDIA/open-gpu-kernel-modules) · [Ubuntu ports (noble)](http://ports.ubuntu.com/ubuntu-ports/)  

*系统级变更请在维护窗口操作并自备回滚；本文非正式官方支持说明。*